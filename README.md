# SH-Marketplace

**SH-Marketplace** is a decentralized application (DApp) designed to **certify second-hand goods** on the blockchain.

## 📂 Project Structure

* **backend/** – Contains the smart contracts written with **Hardhat**.
* **frontend/** – The web application that interacts with the smart contracts using **Wagmi**.

---

## ⚙️ Setup Instructions

### 1. Clone the repository

```bash
git clone https://github.com/Maxpro-dk/sh-maketplace.git
cd sh-maketplace
```

---

### 2. Run the local blockchain and deploy the contracts

```bash
cd backend
npm install
npm run compile
npm run node       # Starts the local Hardhat node

# Make sure to add the local Hardhat network to your MetaMask wallet.
# Also, import one of the demo accounts generated by Hardhat into MetaMask.


npm run deploy   # Deploys the contracts to the local node
```

---

### 3. Start the frontend application

```bash
cd ../frontend
npm install
npm run dev
```

---

## 🔑 Requirements

* [Node.js](https://nodejs.org/) (v16 or higher)
* [MetaMask](https://metamask.io/) browser extension installed and connected to your local network

---

## 🧾 Notes

Make sure MetaMask is configured to connect to the **local Hardhat network** before interacting with the DApp.


